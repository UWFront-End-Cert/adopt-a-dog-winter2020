function totalAdder() {
  let input = prompt('enter price here');
  let priceToAdd = 100;
  let result = 0;

  result = priceToAdd + Number(input);
  alert('Welcome to your new pet! Your total is now' + '$' + result);
}
